interface:
  display_name: "InboxJarvis API CLI"
  short_description: "Manage InboxJarvis rules and stats"
  default_prompt: "Use $inbox-zero-api to inspect or update InboxJarvis rules and analytics through the API CLI."

policy:
  allow_implicit_invocation: true
